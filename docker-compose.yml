services:
  db:
    image: postgres:15
    container_name: postgres_prod
    restart: unless-stopped
    volumes:
      - /mnt/quidpath-data-prod/postgres:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - quidpath_network

  redis:
    image: redis:alpine
    container_name: redis_prod
    restart: unless-stopped
    volumes:
      - /mnt/quidpath-data/redis:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - quidpath_network

  backend:
    image: ${IMAGE_TAG}
    container_name: quidpath-backend-prod
    restart: unless-stopped
    ports:
      - "127.0.0.1:8004:8004"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      DB_NAME: ${DB_NAME:-${POSTGRES_DB}}
      DB_USER: ${DB_USER:-${POSTGRES_USER}}
      DB_PASSWORD: ${DB_PASSWORD:-${POSTGRES_PASSWORD}}
      DB_HOST: ${DB_HOST:-db}
      DB_PORT: ${DB_PORT:-5432}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT}
      USE_MEMORY_CHANNEL_LAYER: ${USE_MEMORY_CHANNEL_LAYER}
      DJANGO_SETTINGS_MODULE: quidpath_backend.settings.prod
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      ALLOWED_HOSTS: ${ALLOWED_HOSTS}
      CSRF_TRUSTED_ORIGINS: ${CSRF_TRUSTED_ORIGINS}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      BILLING_SERVICE_URL: http://billing-backend-prod:8000
      BILLING_SERVICE_SECRET: ${BILLING_SERVICE_SECRET}
      BILLING_WEBHOOK_SECRET: ${BILLING_WEBHOOK_SECRET}
      FRONTEND_URL: ${FRONTEND_URL:-https://www.quidpath.com}
      PAYSTACK_PUBLIC_KEY: ${PAYSTACK_PUBLIC_KEY}
      PAYSTACK_SECRET_KEY: ${PAYSTACK_SECRET_KEY}
      PAYSTACK_TEST_MODE: "false"
      PAYSTACK_WEBHOOK_URL: "https://billing.quidpath.com/api/billing/webhooks/paystack/"
      PAYSTACK_CALLBACK_URL: "https://www.quidpath.com/payment/callback"
      INVENTORY_SERVICE_SECRET: ${INVENTORY_SERVICE_SECRET}
      POS_SERVICE_SECRET: ${POS_SERVICE_SECRET}
      CRM_SERVICE_SECRET: ${CRM_SERVICE_SECRET}
      HRM_SERVICE_SECRET: ${HRM_SERVICE_SECRET}
      PROJECTS_SERVICE_SECRET: ${PROJECTS_SERVICE_SECRET}
      INVENTORY_SERVICE_URL: http://inventory-backend:8000
      POS_SERVICE_URL: http://pos-backend:8000
      CRM_SERVICE_URL: http://crm-backend:8000
      HRM_SERVICE_URL: http://hrm-backend:8000
      PROJECTS_SERVICE_URL: http://projects-backend:8000
    volumes:
      - /mnt/quidpath-data/media:/app/media
      - /mnt/quidpath-data/staticfiles:/app/staticfiles
    # healthcheck:
    #   test: ["CMD-SHELL", "curl -f http://localhost:8004/api/auth/health/ || exit 1"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
    #   start_period: 40s
    networks:
      - quidpath_network

networks:
  quidpath_network:
    name: tmp_quidpath_network
    driver: bridge

volumes:
  postgres_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/quidpath-data/postgres
